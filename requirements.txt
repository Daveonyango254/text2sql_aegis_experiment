# Pinned for reproducibility on RunPod PyTorch 2.4 / CUDA 12.4 images.
# torch ships with the RunPod image — do NOT reinstall it here.
transformers==4.48.3
trl==0.13.0
peft==0.14.0
bitsandbytes==0.45.0
accelerate==1.3.0
datasets==3.2.0
huggingface_hub>=0.27
sqlglot>=25.0
python-dotenv>=1.0
papermill>=2.6
jupyter
nbformat>=5.10
pyyaml>=6.0
sentencepiece
protobuf
einops
# optional speedup (compile takes ~10 min; sdpa fallback works without it):
# flash-attn --no-build-isolation
